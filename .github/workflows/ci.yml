name: CI

on:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Cache virtualenv
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ runner.os }}-py3.12-${{ hashFiles('poetry.lock') }}

      - name: Install dependencies
        run: poetry install --no-interaction --no-root

      # The four checks below mirror the former .pre-commit-config.yaml hooks.
      - name: ruff check
        run: poetry run ruff check src

      - name: black check
        run: poetry run black --check src

      - name: mypy
        run: poetry run mypy --config-file pyproject.toml src

      - name: pytest
        run: poetry run pytest tests --ignore=tests/test_claude_chrome --tb=short
